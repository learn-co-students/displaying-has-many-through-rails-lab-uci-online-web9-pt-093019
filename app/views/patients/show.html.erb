<h1>Patient: <%= @patient.name %></h1>
<br />
<p>Appointments:</p>

<ul>
<% @patient.appointments.each do |appt| %>
    <li><%= link_to "Date & Time: #{appt.appointment_datetime.strftime("%B %d, %Y at %k:%M")} Doctor: #{appt.doctor.name}", doctor_path(appt.doctor) %></li>
<% end %>
</ul>